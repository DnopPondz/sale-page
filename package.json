{
  "name": "minimal-shop",
  "private": true,
  "workspaces": ["apps/*", "server", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/client\" \"npm run dev --workspace=apps/admin\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=apps/client && npm run build --workspace=apps/admin && npm run build --workspace=server",
    "seed": "npm run seed --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
